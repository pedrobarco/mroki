name: ci

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
    tags: ["v*"]

permissions:
  contents: write
  packages: write

jobs:
  # ── Lint · Build · Test ──────────────────────────────────────────────

  go:
    name: go
    uses: ./.github/workflows/_go.yaml
    with:
      targets: >-
        [
          "cmd/mroki-api",
          "cmd/mroki-proxy"
        ]

  pnpm:
    name: pnpm
    uses: ./.github/workflows/_pnpm.yaml
    with:
      targets: >-
        [
          "web/mroki-hub",
          "web/mroki-site"
        ]

  # ── Deploy docs site to GitHub Pages ──────────────────────────────────

  pages:
    name: pages
    # Publish on merge to main only; PRs still build via the pnpm job.
    # Add entries to serve more sources under subpaths, e.g. a hub demo:
    #   { "name": "hub", "dir": "web/mroki-hub", "dist": "dist", "subpath": "hub" }
    if: github.ref == 'refs/heads/main'
    needs: [pnpm]
    permissions:
      contents: read
      pages: write
      id-token: write
    uses: ./.github/workflows/_pages.yaml
    with:
      sources: >-
        [
          {
            "name": "site",
            "dir": "web/mroki-site",
            "dist": ".vitepress/dist",
            "subpath": "."
          }
        ]

  # ── Push Docker images ────────────────────────────────────────────────

  docker:
    name: docker
    if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')
    needs: [go, pnpm]
    uses: ./.github/workflows/_docker.yaml
    with:
      targets: >-
        [
          "build/package/mroki-api",
          "build/package/mroki-proxy",
          "build/package/mroki-hub",
          "build/package/mroki-db-migrator"
        ]
    secrets: inherit

  # ── Helm ──────────────────────────────────────────────────────────────

  helm:
    name: helm
    if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')
    needs: [docker]
    uses: ./.github/workflows/_helm.yaml
    with:
      targets: >-
        [
          "deployments/kubernetes/charts/mroki"
        ]
    secrets: inherit

  # ── Release ───────────────────────────────────────────────────────────

  release:
    name: release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [helm]
    uses: ./.github/workflows/_release.yaml
    secrets: inherit
