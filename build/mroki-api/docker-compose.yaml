services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - ../../internal/infrastructure/persistence/postgres/schema.sql:/docker-entrypoint-initdb.d/init.sql
